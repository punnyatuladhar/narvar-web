package avecode.services;
/**
 * Written for pwc hp project with interface
 * @author silver
 *
 */
import javax.ws.rs.Consumes;
import javax.ws.rs.FormParam;
import javax.ws.rs.GET;
import javax.ws.rs.Path;
import javax.ws.rs.PathParam;
import javax.ws.rs.Produces;
import javax.ws.rs.QueryParam;
import javax.ws.rs.core.MediaType;
import javax.ws.rs.core.Response;

import avecode.model.ui.ProductRequest;
import javax.ws.rs.core.Response;

/**
 * An interface to generate and view reports from tableau server	
 *
 */
@Path("reports/v1")
@Produces(MediaType.APPLICATION_XML)
public interface IReportService {

	/**
	 * Authenticates a user on tableau server
	 * @param req
	 * @return
	 */
	@GET
	@Path("signin")
	@Consumes(MediaType.APPLICATION_XML)
	public Response signIn(String req);
	
	/**
	 * Returns a report preview image for given site and workbook
	 * @param site
	 * @param workBook
	 * @return
	 */
	@GET
	@Path("reportpreview")
	public Response getReportPreview(@PathParam("site") String site, @PathParam("workbook") String workBook);
}
